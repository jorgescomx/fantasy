services:
  fantasy-lab:
    image: jorgesco/fantasy:latest
    build:
      context: .
      dockerfile: Dockerfile
    container_name: fantasy-projection-lab
    restart: unless-stopped
    ports:
      - "8083:4477"
    environment:
      - PORT=4477
      - NODE_ENV=production
      - AUTH_USER=admin
      - AUTH_PASSWORD=fantasy
      # Optional: Uncomment to configure ESPN credentials directly via environment variables
      # - ESPN_SEASON=2026
      # - ESPN_LEAGUE_ID=1541192778
      # - ESPN_SWID={YOUR-SWID-COOKIE}
      # - ESPN_S2=YOUR-ESPN-S2-COOKIE
      # Optional: Google Gemini API key for AI Scout & Chat (free at aistudio.google.com)
      # - GEMINI_API_KEY=AIzaSy...
    volumes:
      # Persistent configuration & history files across container recreation
      - ./app/config.json:/app/config.json
      - ./app/formula_config.json:/app/formula_config.json
      - ./app/learning_history.json:/app/learning_history.json
      - fantasy-data:/app/data

volumes:
  fantasy-data:
